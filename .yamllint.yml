extends: default

rules:
  line-length:
    max: 100
    level: warning
  indentation:
    spaces: 2
    level: warning
  quoted-strings:
    level: warning
    required: only-when-needed
  trailing-spaces: enable
  truthy: true
